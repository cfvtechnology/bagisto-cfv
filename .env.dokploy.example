# ============================================================================
# Bagisto Environment Variables for Dokploy
# ============================================================================
# Copy these variables to your Dokploy service environment configuration
# Update the values according to your deployment requirements
# ============================================================================

# ----------------------------------------------------------------------------
# Application Configuration
# ----------------------------------------------------------------------------

# Your application URL (REQUIRED - update with your domain)
APP_URL=https://tienda.cfv.technology

# Application environment (production, staging, local)
APP_ENV=production

# Debug mode (set to false in production)
APP_DEBUG=false

# Application timezone
APP_TIMEZONE=America/Mexico_City

# Application locale
APP_LOCALE=es
APP_FALLBACK_LOCALE=en

# Default currency
APP_CURRENCY=MXN

# ----------------------------------------------------------------------------
# Database Configuration
# ----------------------------------------------------------------------------

# Database host (use service name from docker-compose.yml)
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=bagisto
DB_USERNAME=root
DB_PASSWORD=root

# IMPORTANT: Change DB_PASSWORD to a strong password in production!

# ----------------------------------------------------------------------------
# Bagisto Automated Setup Configuration
# ----------------------------------------------------------------------------

# Enable automatic setup on first container start (recommended: true)
AUTO_SETUP=true

# Bagisto version to install
BAGISTO_VERSION=v2.3.6

# Load sample product data (recommended: false for production, true for demo)
SEED_SAMPLE_DATA=false

# Create testing database (recommended: false for production)
CREATE_TEST_DB=false

# Force reinstallation (WARNING: This will delete existing data!)
# FORCE_SETUP=false

# Skip setup completely (use if you want to install manually)
# SKIP_SETUP=false

# ----------------------------------------------------------------------------
# Redis Configuration
# ----------------------------------------------------------------------------

REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=null

# Cache configuration
CACHE_STORE=redis
SESSION_DRIVER=redis
QUEUE_CONNECTION=redis

# ----------------------------------------------------------------------------
# Mail Configuration (Mailpit for development/testing)
# ----------------------------------------------------------------------------

MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS=noreply@cfv.technology
MAIL_FROM_NAME="CFV Technology Store"

# For production, use a real mail service:
# MAIL_MAILER=smtp
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password
# MAIL_ENCRYPTION=tls

# ----------------------------------------------------------------------------
# Admin Configuration
# ----------------------------------------------------------------------------

ADMIN_MAIL_ADDRESS=admin@cfv.technology
ADMIN_MAIL_NAME="CFV Admin"

# ----------------------------------------------------------------------------
# Elasticsearch Configuration
# ----------------------------------------------------------------------------

ELASTICSEARCH_HOST=elasticsearch
ELASTICSEARCH_PORT=9200

# ----------------------------------------------------------------------------
# Additional Laravel Configuration
# ----------------------------------------------------------------------------

# Logging
LOG_CHANNEL=stack
LOG_LEVEL=error

# Session
SESSION_LIFETIME=120
SESSION_ENCRYPT=false

# Broadcasting
BROADCAST_CONNECTION=log

# Filesystem
FILESYSTEM_DISK=public

# ----------------------------------------------------------------------------
# Security (Production)
# ----------------------------------------------------------------------------

# IMPORTANT: Generate a new APP_KEY after installation
# Run: docker exec <container> php artisan key:generate
# APP_KEY=base64:your-generated-key-here

# ----------------------------------------------------------------------------
# Optional: Performance Tuning
# ----------------------------------------------------------------------------

# Enable response cache (recommended for production)
RESPONSE_CACHE_ENABLED=true

# Opcache settings (already configured in PHP, but can be adjusted)
# See php.ini for opcache configuration

# ----------------------------------------------------------------------------
# Optional: External Storage (S3, etc.)
# ----------------------------------------------------------------------------

# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=us-east-1
# AWS_BUCKET=
# AWS_USE_PATH_STYLE_ENDPOINT=false

# ----------------------------------------------------------------------------
# Dokploy-Specific Notes
# ----------------------------------------------------------------------------

# 1. Copy these variables to Dokploy UI: Service → Environment Variables
# 2. Update APP_URL with your actual domain
# 3. Change DB_PASSWORD to a secure password
# 4. Configure your domain in Dokploy → Domains section
# 5. Enable HTTPS (Dokploy handles SSL certificates automatically)
# 6. After first deployment, change admin credentials immediately
# 7. Generate a new APP_KEY for security

# Default Admin Credentials (CHANGE IMMEDIATELY after first login):
# Email: admin@example.com
# Password: admin123

# ============================================================================
# End of Configuration
# ============================================================================
